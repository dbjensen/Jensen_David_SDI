// Prompt and Alert

var width = prompt("We are clculating the area of a rectangle.  \nPlease enter the width");
var height = prompt("Please enter the heigth");
var area = width * height;                          // Calculates with that information
// console.log(area);                                  // Prints it out to console
var result = "The area of your rectangle is " + area + " sp feet";
alert(result);